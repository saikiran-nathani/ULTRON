import { Placeholder } from "./Placeholder";

export function CaptureScreen() {
  return <Placeholder title="Capture" note="One box, one tap, straight to the inbox. Triage later — and it drains to the vault on the next harvest." />;
}
