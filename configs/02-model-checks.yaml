# Phase 2 (slides 27-28) — tokenizer length measurement + chat template verification.
# Committed BEFORE running, per slide 13.

base_model: Qwen/Qwen2.5-Coder-0.5B-Instruct   # OVERRIDES.md: student is Instruct

template:
  name: qwen-chatml-v1        # version it; changing this = changing the experiment
  system_prompt: nsk         # decide ONCE. Must be identical in baseline and every
                              # post-SFT run, or the first "improvement" is a prompt change.

measure:
  # Custom curated set, built by hand. Not present yet -- the script exits with a
  # clear message until it is. Row shape: data/README.md
  source: data/interim/sft-candidates.jsonl
  prompt_field: prompt
  response_field: response
  seq_len_candidates: 1024        # scalar or list; a list gives a comparison table
                                  # CLAUDE.md: 1024 comfortable, 2048 batch 1